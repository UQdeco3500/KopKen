import React from 'react'
import PropTypes from 'prop-types'
import { SafeAreaView, Text, View } from 'react-native'

function Reminisce(props) {
    return (
        <SafeAreaView>
            <View>
                <Text>This is reminisce</Text>
            </View>
        </SafeAreaView>
    )
}

Reminisce.propTypes = {}

export default Reminisce
